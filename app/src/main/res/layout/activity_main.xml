<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="com.example.kaushal.studentsearch.MainActivity">

   <RelativeLayout
       android:layout_width="match_parent"
       android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

    <EditText
        android:id="@+id/edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/search_hint"
        android:maxLines="1"
        android:elevation="5dp"
        android:padding="16dp" />
    <Spinner
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/spinner_hall"
        android:entries="@array/hall_entries"
        android:entryValues="@array/hall_values" />

        <Spinner
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/spinner_gender"
            android:entries="@array/gender_entries"
            android:entryValues="@array/gender_values" />

       </LinearLayout>

       <Button
           android:id="@+id/button_go"
           style="@style/Widget.AppCompat.Button.Colored"
           android:layout_width="wrap_content"
           android:layout_height="wrap_content"
           android:layout_alignParentBottom="true"
           android:layout_centerHorizontal="true"
           android:layout_marginBottom="141dp"
           android:hint="@string/button_text"
           android:paddingBottom="25dp"
           android:paddingLeft="50dp"
           android:paddingRight="50dp"
           android:paddingTop="25dp"
           android:text="@string/button_text" />




    </RelativeLayout>

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/bottomSheetLayout"
        android:layout_width="match_parent"
        android:layout_height="350dp"
        android:background="@android:color/holo_blue_light"
        android:padding="16dp"
        app:behavior_hideable="true"
        app:behavior_peekHeight="0dp"
        app:layout_behavior="@string/bottom_sheet_behavior">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/hall_spinner_label"
            android:text="@string/selectHall"
            style="@style/Widget.AppCompat.TextView.SpinnerItem"/>

        <Spinner
            android:layout_toRightOf="@id/hall_spinner_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/hall_spinner"
            android:prompt="@string/selectHall"
            android:entries="@array/hall_entries"
            android:layout_marginLeft="16dp"/>

    </RelativeLayout>

</android.support.design.widget.CoordinatorLayout>